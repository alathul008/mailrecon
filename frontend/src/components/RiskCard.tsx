import {ShieldAlert} from 'lucide-react'; import {Badge} from './Badge';
export function RiskCard({score,level}:{score:number|null;level:string|null}){const tone=level==='CRITICAL'||level==='HIGH'?'danger':level==='MEDIUM'?'warn':'good';return <div className="glass rounded-2xl p-5"><div className="flex items-center justify-between"><div className="text-sm text-zinc-400">Risk posture</div><ShieldAlert size={17} className="text-red-400"/></div><div className="mt-4 flex items-end gap-3"><div className="text-4xl font-semibold tracking-tight">{score??'—'}</div><div className="pb-1 text-sm text-zinc-500">/ 100</div><Badge tone={tone}>{level??'PENDING'}</Badge></div><div className="mt-4 h-1.5 overflow-hidden rounded-full bg-white/8"><div className="h-full rounded-full bg-red-400 transition-all" style={{width:`${score??0}%`}}/></div></div>}
