import { Construction } from 'lucide-react';
export function Placeholder({title}:{title:string}){return <div className="mx-auto flex min-h-[70vh] max-w-3xl items-center justify-center p-8"><div className="glass w-full rounded-3xl p-10 text-center"><Construction className="mx-auto text-red-400" size={28}/><div className="mt-5 text-xl font-semibold">{title}</div><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-zinc-500">This workspace is intentionally kept separate from the core investigation flow. The current release prioritizes evidence integrity, provider transparency and reproducible reports.</p></div></div>}
